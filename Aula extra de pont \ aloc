#include <iostream>

using namespace std;

/*void leValores(int n, int *soma, int *media,int *maior, int *menor)
{
    int valor;
    cout << "Digite os valores: " << endl;
    cin >> valor;
    *soma = valor;
    *media = 0;
     *maior = valor;
    *menor = valor;
    for ( int i = 0; i < n-1; i++){
            cin >> valor;
        *soma += valor;
        if( *maior < valor)
        {
            *maior = valor;
        }
        if(*menor > valor)
        {
            *menor = valor;
        }
    }

    *media = *soma/n;

}*/

/*int* intercala(int n, int v1[], int v2[])
{
    int *resultado = new int[n];

    for(int i = 0; i < 2*n;i++)
    {
        if(i%2==0){
            *(resultado + i) = *(v1 +(i/2));

           } else{
            *(resultado + i) = *(v2+(i/2));

            }


    }

    return resultado;

}*/



void acimaDaMedia(int n)
{
  float valores = 0;
  float media = 0;
  int qnt = 0;
  int *vet = new int[n];
  cout << "Digite os valores: ";
  for(int i = 0; i < n; i++){
    cin >> valores;
    *(vet + i) = valores;
    media += valores;
  }
  media = media/n;
  cout << "Media: " << media << endl;
   for(int i = 0; i < n; i++){
    if(*(vet + i) > media)
    {
        qnt++;
        cout << "Valor acima da media: " << *(vet + i) << endl;
    }
  }
    cout << "Quantidade acima da media: " << qnt;
    delete [] vet;
}



int main() {
  /*  int n, soma, maior, menor;
    int media;

    cout << "Quantos valores deseja ler? ";
    cin >> n;

    if (n <= 0) {
        cout << "Número inválido!" << endl;
        return 1;
    }

    leValores(n, &soma, &media, &maior, &menor);

    cout << "\n--- Resultados ---" << endl;
    cout << "Soma: " << soma << endl;
    cout << "Média: " << media << endl;
    cout << "Maior: " << maior << endl;
    cout << "Menor: " << menor << endl;

*/
 /*
    int n;
    cout << "Digite o tamanho do vetor: " ;
    cin >> n;
    int *v1;
    int *v2;
    int *vetInter;
    v1 = new int[n];
    v2 = new int[n];

    cout << "Digite os valores do vetor 1: " << endl;
    for(int i =0 ; i < n;i++)
    {
     cin >> v1[i];

    }
 cout << "Digite os valores do vetor 2: " << endl;
    for(int i =0 ; i < n;i++)
    {
     cin >> v2[i];

    }
    vetInter = intercala(n,v1,v2);

for(int i =0 ; i < n;i++)
    {
     cout << vetInter[i] << " ";

    }
    delete [] vetInter;
    delete [] v1;
    delete [] v2;*/

    int n = 0;
    cout << "Digite quantos valores quer fazer: ";
    cin >> n;
    acimaDaMedia(n);

    return 0;
}

