#include <iostream>

using namespace std;

/*float prodEscalar(int n, float x[], float y[])
{
        float resultado = 0;
        for ( int i = 0; i < n; i++){

            resultado += x[i]*y[i];
        }

    return resultado;
}*/
//ATIVIDADE (3)

/*int* indicesPares(int vet[], int n)
{
 int n_novo = n/2;
    if(n_novo%2!= 0){
        n_novo++;
    }

    int *pares = new int [n];
     for( int i =0; i < n;i++)
     {
        if(i%2==0){
            *(pares + i) = vet[i];
        }
     }

    return pares;
}*/
//ATIVIDADE (4)

int* redimensiona(int vet[], int tam,int novo)
{
    if(tam == novo || tam > novo){
        return vet;
    }else{


    }



}


int main()
{
/*
    //ATIVIDADE 1

    int n;

    cout << "Digite o seu numero inteiro: " << endl;

    cin >> n;

    int *vet = new int [n];

    int somador= 0;


    cout << "Digite os valores do vetor: " << endl;
    for(int i = 0; i < n;i++)
    {
        cin >> *(vet + i);
        somador += *(vet + i);

    }
    delete [] vet;

    cout << "A media dos elementos são: " << somador/n << endl << endl;


    //ATIVIDADE 2

    cout << "Digite os 3 valores de X: " << endl;
    float *vetx = new float [3];
     for(int i = 0; i < 3;i++)
    {
        cin >> vetx[i];

    }
    cout << "Digite os 3 valores de Y: " << endl;
    float *vety = new float [3];
        for(int i = 0; i < 3;i++)
    {
        cin >> vety[i];

    }
    float prod = prodEscalar(3.0,vetx,vety);
    delete [] vetx;
    delete [] vety;
    cout << "O produto escalar é : " << prod << endl << endl;
 */
    //ATIVIDADE 3
/*
    int num;
    cout << "Digite o tamanho do vetor: " << endl;
    cin >> num;
    int vet[num];
    cout << "Digite os valores do vetor: " << endl;
    for (int i = 0; i <num; i++){
        cin >> vet[i];
    }

    int *vetPares = indicesPares(vet,num);
    cout << "O novo vetor é: " << endl;
    for(int i = 0;i < num/2;i++){
        cout << "Indice " << i << " valor: " << vetPares[i] << endl;
    }

    delete [] vetPares;
    */


    //TEM ALGO ERRADO!!!! ARRUMAR

    //ATIVIDADE 4
/*
    int n1;
    cout << "Digite o tamanho do primeiro vetor: " << endl;
    cin >> n1;
    int vetor[n1];
    cout << "Digite o tamanho do proximo vetor: " << endl;
    int n2;
    cin >> n2;

        int *novovetor = redimensiona(vetor,n1,n2);*/


    int **mat;
    int n;

    cout << "Digitar ordem: ";

    cin >> n;

    mat = new int*[n];

for(int i=0; i < n;i++){
        mat[i] =new int[n];
    }

    cout << "Digite os elementos da matriz: " << endl;
    for(int i=0;i < n; i++){
        for(int j =0; j < n; j++){
            cout << "mat[" <<i << "][" << j << "]";
            cin >>mat[i][j];
        }
        cout << endl;
    }
    for(int i=0; i < n;i++){
        delete mat[i];
    }
    delete [] mat;


    return 0;
}
